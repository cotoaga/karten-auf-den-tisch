/* ESLint config — minimal for Next.js 16 flat-config mode.
 * Type checking is the primary quality gate (tsc --noEmit / npm run lint).
 * eslint-config-next is retained for IDE integration only. */
export default [];
